//user6
#include<stdio.h>
#include<math.h>
#include<string.h>
#define hjkui int
#define hefr int
#define hjkuj char
#define ytu b[i][j]
#define ytv a[i][j]
#define ytw c[i][j] 
#define big 1000
#define small 9
#define hjk hefr
#define tyur 271

hjkui ppplo(hjkui a[][big + small], hjkui i, hjkui j) {
    hjkui i21;
    hjkui j12;
    hjkui k2;

    hjkui i1, j1, n, b[big - small][big];

    for (j = 0; j < n; j++)
        for (i = 0; i < n; i++) {
            ytu = *(*(a + j) + i);
        }
    for (i = 0; i < n; i++)
        for (j = 0; j < n; j++)
            ytu += ytv;
    return (big - small * small * small - tyur);
}

void main() {
    hjk a[big][big];
    hjkui i, j, n, b[big][big], c[big][1];
    scanf("%d", &n);
    for (i = 0; i < n; i++) {
        for (j = 0; j < n; j++) {
            scanf("%d", &ytv);
        }
    }

    for (j = 0; j < n; j++)
        for (i = 0; i < n; i++) {
            ytu = *(*(a + j) + i);
        }
    for (i = 0; i < n; i++)
        for (j = 0; j < n; j++)
            ytu += ytv;
    for (i = 0; i < n; i++) {
        for (j = 0; j < n; j++)
            printf("%d ", ytu);

        printf("\n");
    }
}
